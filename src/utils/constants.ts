import { type CSSProperties } from 'react';

/** 相同引用空的`react css style`以防多余渲染 */
export const emptyStyle: CSSProperties = {};

/** 定义常数`UNDEFINED`以防与`undefined`关键字冲突 */
export const UNDEFINED = void 0;
